tasks.named("bootJar") {
    enabled = true
}

tasks.named("jar") {
    enabled = false
}

dependencies {
    // module
    runtimeOnly project(':infra:database')
    implementation project(':domain')

    // web
    implementation 'org.springframework.boot:spring-boot-starter-web'
}